[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
	integration: optional integration tests for GUI, tray, and camera runtime paths
	requires_display: needs a graphical DISPLAY/Tk environment
	requires_camera: needs a real camera device and OpenCV access
